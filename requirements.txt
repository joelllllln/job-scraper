requests>=2.31
PyYAML>=6.0
python-jobspy>=1.1.80    # boards.py only — LinkedIn/Indeed/Glassdoor/Google

# render.py only — reads careers pages whose listings are drawn by JavaScript.
# 86% of firms with no discoverable ATS are in that category. Optional: every
# other stage runs without it, and render.py says so plainly if it is missing.
# After installing:  playwright install chromium
playwright>=1.40 ; python_version >= "3.9"
